#pragma once
#include <vector>

// Declare your sorted_squared interface here.
std::vector<int> sortedSquared(const std::vector<int>& array);
std::vector<int> sortedSquaredFaster(const std::vector<int>& array);
